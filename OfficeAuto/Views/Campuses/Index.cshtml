
@{
    ViewData["Title"] = "Campuses";
}
<ol class="breadcrumb">
    <li class="breadcrumb-item">Home</li>
   
    <li class="breadcrumb-item active">Campuses</li>

    
</ol>
<div class="container-fluid">

    <div class="animated fadeIn">
        <div class="card">
            <div class="card-header">
                <i class="fa fa-building"></i> Campuses
                <div class="card-header-actions">
                    <a asp-action="Create" class="btn btn-sm btn-success">Create New</a>
                </div>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-sm-12">
                        <table id="dataTables" class="table table-striped table-bordered">
                            <thead>
                                <tr>
                                    <th>
                                        Name
                                    </th>
                                    <th>
                                        CampusCode
                                    </th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    <td>
                                        BU Islamabad
                                    </td>
                                    <td>
                                        BU-ISB
                                    </td>
                                    <td>
                                        <a class="btn btn-info" href="/Campuses/Edit/3"><i class="fa fa-edit"></i></a>
                                        <a onclick="return confirm('Are you sure want to delete this?');" class="btn btn-danger" href="#"><i class="fa fa-trash-o"></i></a>

                                    </td>
                                </tr>
                            </tbody>

                        </table>
                        @*<table id="dataTables_1" class="table table-striped table-bordered">
        <thead>@model IEnumerable<OfficeAuto.Models.DB.Campuses>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CampusCode)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.CampusCode)
                    </td>
                    <td>
                        <a class="btn btn-info" asp-action="Edit" asp-route-id="@item.Id"><i class="fa fa-edit"></i></a>
                        <a onclick="return confirm('Are you sure want to delete this?');" asp-action="Delete" class="btn btn-danger" asp-route-id="@item.Id"><i class="fa fa-trash-o"></i></a>

                    </td>
                </tr>
            }
        </tbody>*@

                        </table>
                    </div>
                </div>

            </div>
        </div>
    </div>
</div>

